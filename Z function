def Z_FUNCTION(s):
    n = len(s)
    z = [0]*n
    l = r = 0
    for i in range(1, n):
        if i <= r:
            z[i] = min(z[i-l], r-i+1)
        
        while i + z[i] < n and s[z[i]] == s[i+z[i]]:
            z[i] += 1

        if i + z[i]-1 > r:
            l = i
            r = i+z[i]-1
    
    return z


z = Z_FUNCTION(s)

# z[i] tells you how many characters from position i match the prefix of the string.
